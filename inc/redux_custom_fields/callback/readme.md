```php

$this->sections[] = [
			'id'               => 'my_section',
			'fields' => [
				[
					'id'       => 'custom_field_test',
					'type'     => 'callback',
					'title'    => 'Custom Field',
					'callback' => 'my_callback',
				]
			]
]

```

